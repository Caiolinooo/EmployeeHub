# Gestão de Tripulantes UI — DOX

## Purpose

Componentes da Matriz, modal do colaborador e Man Schedule. Lookups profissionais compartilhados e viewport da escala.

## Ownership

- `src/components/gestao-tripulantes/`
- Lookups: `SearchableCreatableSelect.tsx`, `createGtLookupOption.ts`
- KPIs: consomem `GET /api/gestao-tripulantes/dashboard` (`dashboard-service.ts`)
- Filtro de data da escala: `ScheduleDateFilterInput.tsx` + `src/lib/gestao-tripulantes/filter-date.ts`
- ASO logistics inbox: `AsoAgendamentoInbox.tsx` (aba GT **ASO Logística**)
- ASO DP panel: `AsoAgendamentoDpPanel.tsx`
- Admin ASO config: `admin/AsoAgendamentoConfigTab.tsx`
- Grade da escala: `man-schedule-grid-classes.ts` (scroll + sticky) usado por `GTManScheduleTab` e `/department/man-schedule`
- Viewport das páginas GT-family: `GtPageShell.tsx` (`/department/gestao-tripulantes`, `/department/man-schedule`, `/department/dp`, `/department/e-social`)
- Modal do colaborador: `collaborator-modal-layout.ts` (viewport + tablist + table scrollports) usado por `CollaboratorModal` e todas as abas

## Local Contracts

- Cargo / Empresa / Embarcação / Centro de Custo: `SearchableCreatableSelect` (busca + Adicionar). Create via POST `/api/gestao-tripulantes/{cargos|empresas|embarcacoes|centros-custo}` (`createGtLookupOption`). Filtros da matriz/Man Schedule: busca sem create (valores de nome, não UUID).
- **Cadastro completo** (`ColaboradorCadastroForm`): create/edit de todos os campos de `gt_colaboradores`. Usado em `/department/dp/novo`, `/department/gestao-tripulantes/novo` e `DadosPessoaisTab` (modo Editar, `embedded`). POST/PUT na API interna. Sem store paralelo.
- **Regime de trabalho** (`ColaboradorCadastroForm` / `DadosPessoaisTab`): opções `sem_escala` / `administrativo` / `onshore` + NxN. Não defaultar 14x14. Sem rotação grava dias 0; a grade Man Schedule não inventa ON. Token vazio + `escala_* = 0` lê como `sem_escala` (`inferRegimeUi`).
- **Matrícula e-Social**: campo editável `matricula_esocial` (além de `matricula`). Envio S-2220 lê `matricula_esocial || matricula`. Vazio no save copia `matricula`.
- Cards da Matriz: só `ativo=true` e CC ativo; docs vencidos do card e das badges = **primário por grupo** (`documento-historico.ts`), não irmãos obsoletos. Coluna Documentos mostra título/tipo do vigente vencido. Filtro **Docs Vencidos** abre `DocsAlertasPanel`.
- Abas do modal (contrato): Dados Pessoais = cadastro RH (sem docs ASO/EPI); Ficha unificada = Employee Hub; Treinamentos = certificados/cursos; **ASO** = único lugar de exames ocupacionais/laudo (upload, S-2220, agendamento DP); Passaportes = viagem (não ocultar por cargo); Documentos = só `gt_documentos` agrupados; **QHSE / EPI** = ficha AN-HSE-005 + EPI + listas QHSE, **nunca ASO**; **Desligamento** = rescisão (`gt_desligamentos`) — botão no header para quem `pode_registrar`, histórico readonly se já desligado. Filtro `documentoPertenceAba` (`certificado`→treinamentos, `aso`/`laudo`→aso). Highlight `gt-doc-<id>`.
- Desligamento: `DesligamentoModal` (z-60 sobre o CollaboratorModal). POST fail-soft na folha; após sucesso atualiza `ativo=false` no estado local e chama `onUpdated?` (opcional — DP/GT não precisam mudar a page).
- **POB / Embarcados Agora**: só código de escala **exato `ON`** no dia civil de hoje (`embarque-status.ts`). `ON*`, `*`, STB, DBA, FI, OFF-C, TRE, FER, UTR, DHC não entram.
- **Pílula Status da Matriz / DP / ficha**: mesma célula de hoje. ON → Embarcado (verde); STB → StandBy (laranja); Folga/OFF/OFF-C/FI → Folga (azul); FER/AFAST → Afastado (vermelho). `GET /colaboradores` sobrescreve `status_embarque` com esse mapa (`escala_codigo_hoje`). Não usar só a coluna stale.
- Cards de KPI são botões: `?kpi=embarcados|disponiveis|docs_vencidos|colaboradores` filtra a lista (e Man Schedule para embarcados/STB). Clique de novo limpa. Wrapper `data-gt-kpi-cards`: desktop `grid-cols-2 lg:grid-cols-4`; ≤767 (`globals.css`) vira faixa horizontal com snap — não corta a 2ª linha no overflow do `GtPageShell`.
- Tablist do CollaboratorModal: desktop = portal (`overflow-x-auto` no inner `role=tablist`, `tablistRef` no inner). Mobile (`max-lg:`): shell vira scrollport (`min-w-0 overflow-x-auto`); inner `min-w-max` + `overflow-x-visible`. Overflow e `min-w-max` em nós distintos.
- Man Schedule: checkbox `Visualizar por dia` (`gt-man-schedule-viewport-day`). Off = semana sáb–sex; on = um dia por coluna.
- Toolbar **Hoje / ‹ › coluna / mês de referência / pill POB** (`ManScheduleTimelineNav`) compartilhada por `GTManScheduleTab` e `/department/man-schedule`. **Mês de referência** (default = mês civil atual; `localStorage` `gt-man-schedule-reference-month`): a grade cobre o mês inteiro (1º–último dia + snap sábado), mesmo sem rotações — permite planejar o futuro. Setas do label (`‹ Setembro 2026 ›`) mudam o mês; distintas das setas que andam **uma coluna**. **Hoje** restaura o mês atual e rola até a coluna de hoje (viewport dia = aquele dia; semana = sáb–sex que contém hoje). Destaque amarelo só quando essa coluna existe na grade (sem nearest-fallback). A pill interpola `{count}` (`manSchedule.todayPob`) via `interpolateTranslationParams`. Contagem = `countPobOnCivilDay` no dia civil de **hoje**, **independente do mês de referência visível**. Colunas via `buildScheduleColumns` (`man-schedule-reference-month.ts`). `/department/man-schedule` amplia `?janela=` (`realtimeJanelaForReferenceMonth`) se o mês sair da janela 90d; aba GT permanece `janela=all`. O scroll usa `[data-man-schedule-col]` no scrollport existente — não alterar overflow/sticky só para os botões.
- **Scroll da grade (global)**: wrapper `overflow-x-scroll overflow-y-auto` + `min-h-0 min-w-0` (`[data-testid=man-schedule-scroll]`, `.man-schedule-scroll`). `min-w-0` é obrigatório em **toda** a cadeia flex (`<main>`, `GtPageShell`, wrapper da aba, root do tab, scrollport) — sem isso `min-width:auto` cresce até a tabela `w-max` e o `body overflow-x-hidden` corta a grade sem barra. Coluna **NOME** (e QTD/CARGO) `position: sticky; left: …` com fundo opaco; `thead` `sticky top-0`. Tabela `border-separate border-spacing-0` — `border-collapse` quebra sticky no Chrome. Mesmo modelo na aba GT e em `/department/man-schedule`. Classes em `man-schedule-grid-classes.ts`. Barra superior sincronizada (`MAN_SCHEDULE_TOP_SCROLL_CLASS` + `useManScheduleScrollSync`) mede `table.scrollWidth`. Hoje/setas rolam **esse** wrapper (uma coluna via `[data-man-schedule-col]`), sem pixel offset fixo. A **página** em volta usa `GtPageShell` (`flush` em `/department/man-schedule`); não alterar overflow/sticky da grade só para caber o viewport. Sem animação de width na grade.
- **Viewport das páginas GT-family**: `GtPageShell` — coluna flex `flex-1 min-h-0 min-w-0` que preenche o `<main>` do MainLayout (`h-dvh` + `flex flex-col` + `min-w-0`). Header/abas/filtros/toolbar `shrink-0`. Em desktop (`lg:`), grade/tabela `flex-1 min-h-0 min-w-0 overflow-auto` (`GT_PAGE_SCROLLPORT_CLASS`). Em mobile (`< 1024px`) o scrollport é `max-lg:flex-none max-lg:min-h-[50vh]` — `flex-1 min-h-0` + `overflow-hidden` com filtros empilhados zerava a lista/escala (`page.tsx`, `GTMatrix.tsx`, `GTManScheduleTab.tsx`). Abas: `GT_PAGE_TABLIST_CLASS` / `GT_PAGE_TABNAV_CLASS` / `GT_PAGE_TAB_BUTTON_CLASS` — `flex-nowrap`, `shrink-0`, `whitespace-nowrap` e `touch-scroll` só com `max-lg:` / `max-md:`; ≥1024 as classes efetivas = portal (`flex space-x-4…` + `pb-3 text-sm font-bold border-b-2 transition-all`). Filtros da escala: desktop `flex items-end gap-2.5 w-full flex-wrap` (igual portal); `max-lg:flex-nowrap` + overflow-x só no celular. Em 1280 a linha fica no limiar do wrap — não trocar `flex-wrap` desktop; diferença de wrap entre builds é métrica de fonte (CSS path do Next 15.5), não classe nova.
- **CollaboratorModal viewport**: painel `h-[min(98dvh,calc(100dvh-0.75rem))]` em coluna flex (`collaborator-modal-layout.ts`). Overlay sem scroll. Header + tablist `shrink-0` (ficam visíveis). Body `[data-testid=collaborator-modal-body]` `flex-1 min-h-0 overflow-auto`. Tablist desktop = portal (overflow no inner + `tablistRef` no inner). Mobile (`max-lg:`): shell scrolla. Botões de ação em modais usam posicionamento sticky no rodapé. `DesligamentoModal`, `ConfirmarExclusaoMarcacaoModal` e `ModalAprovacaoFechamento` fecham com Esc (`useEscapeToClose`).
- Save de evento escreve linha em `allSchedules` (`rotation_start`/`end`). Novo lançamento prevalece sobre STB/ON antigo no mesmo período. `QTD. EMBARC` = pessoas no cargo (não é quantidade de embarques).
- Aba **Ficha unificada**: card **Usuário do portal** lê `portalUser` do hub (nome + e-mail + role). Sem match → "sem vínculo". Join em `src/lib/employee-hub/` (`tax_id` + e-mail, nunca coluna `cpf`).
- Aba **Documentos** do modal: `gt_documentos` locais agrupados por tipo (`documento-historico.ts` + `HistoricoColapsavel`, default fechado). Sem dump de “outros módulos”. Sem QHSE.
- Aba **QHSE / EPI** (`QhseTab`): só se o viewer `hasAccess('epi')` (mesmo módulo do `/epi`). Lista ficha AN-HSE-005, entregas EPI e listas de presença QHSE via catálogo (`onlyQhse`). **Nunca ASO/laudo** — exames ocupacionais ficam na aba ASO. `qhseRelated` é false para `tipo_documento` aso/laudo; `isQhseCatalogDocument` / `?qhse=1` excluem aso mesmo se a fonte marcar `category: 'qhse'`. Não altera Treinamentos/CBSP. Não ocultar Passaportes por cargo.
- Aba **ASO Logística** na página GT: fila `solicitado` para logística aprovar/reprovar com `useSignature` (sem segundo SignatureModal). API: ADMIN/MANAGER **ou** USER cujo setor é logística-like e tem `gestao-tripulantes`. Aprovação marca o ASO; reprovação exige motivo.
- **Fechamento de escalas / Envio DP**: admin `WorkflowFechamentoTab` busca **usuários do portal + colaboradores GT** com e-mail (`listarCandidatosAprovadores` + `SearchableCreatableSelect`). Digitar nome/e-mail filtra. Adicionar/remover e o **Salvar do topo** da página admin gravam `PUT /relatorio-mensal/config` (não o PUT geral). O PUT `/configuracoes` **não** pode sobrescrever `gt_fechamento_mensal_config`. Lista nominada = exatamente essas pessoas assinam, **independente do role**. Lista vazia = um ADMIN/MANAGER conclui. `ModalAprovacaoFechamento` **sempre** `await requestSignature()`. Ator do modal: `useSupabaseAuth` (`profile` + `user` → id, email, role, first_name, last_name). **Nunca** `useAuth` / `@/contexts/AuthContext` — `ClientProviders` só tem `SupabaseAuthProvider` (não ressuscitar `AuthProvider` legado). Preview/tabela mostram ON, DBA, FI, Folga, STB, TRE/FER e check NxN — mesmos campos de `GET /relatorio-mensal` / `calculosFolha`.
- Aba **Treinamentos** / **Passaportes**: mesmo agrupamento. Primário = último certificado válido/permanente; irmãos = Histórico (Obsoleto) com download. Badges do resumo usam só o primário.
- **Matriz de Treinamentos (Admin & Ficha)**:
  - Admin: `src/components/gestao-tripulantes/admin/MatrizTreinamentoConfigTab.tsx` gerencia matrizes ativas, importação de planilhas XLSX oficiais do MIO e inclusão/remoção de requisitos de treinamentos por cargo e regime.
  - Ficha (`TreinamentosTab`): `MatrizConformidadeColaboradorCard.tsx` exibe conformidade percentual do cargo, detalhando cursos vigentes, a vencer, vencidos e faltantes não realizados (com botão direto para lançar).
- **Lista de Presença para Treinamentos Internos**:
  - `ModalListaPresencaTreinamento.tsx`: acessível no topo de `TreinamentosTab` (botão **Lista de Presença**). Permite registrar treinamentos internos, selecionar tripulantes em lote, gerar a lista no portal para assinaturas digitais/PDF e opcionalmente cadastrar a conclusão do curso diretamente no histórico de todos os participantes selecionados.
- **Scroll Sincronizado do Man Schedule**:
  - Estilos em `globals.css` (`.man-schedule-scroll`) com barra visível de 14px, thumb `#94a3b8` e `-webkit-overflow-scrolling: touch`.
  - Hook `useManScheduleScrollSync` (aba GT + `/department/man-schedule`): `ResizeObserver` na tabela **e** no scrollport; spacer da barra superior = `table.scrollWidth`.
  - Barra superior + barra inferior da grade: as duas andam juntas (`scrollLeft`). Desktop arrasta a barra; mobile faz swipe no scrollport.
- **Editar/Excluir itens do cadastro** (Treinamentos, ASO, Documentos, Passaportes): todas as quatro abas usam `useGtDocumentPermissions()` (`use-gt-document-permissions.ts`) para exibir/ocultar os botões `FiEdit2`/`FiTrash2`. `TreinamentosTab` permite edição e exclusão de itens vigentes e obsoletos (histórico); `ASOTab` bloqueia edição/exclusão apenas quando o ASO já foi enviado/processado no e-Social.
- **Filtro Data Início / Data Fim** (`GTManScheduleTab` e `/department/man-schedule`): `ScheduleDateFilterInput` commita só `YYYY-MM-DD` completo com ano 1990–2100 (ou vazio). Chrome `0002-01-01` / `0020-01-01` / `0202-01-01` ao digitar o ano **não** recalcula a grade. Cap: 400 colunas/dia, 2000 semanas (segurança). Matriz/vencimentos/dashboard não têm filtro de período.
- **GT v2 — Workspace do Fechamento (tela cheia, 2026-09-16)**: `ModalAprovacaoFechamento` virou workspace fullscreen com abas (`fixed inset-0 z-50`; mobile `p-0 h-[100dvh] rounded-none`, desktop `sm:h-[min(98dvh,calc(100dvh-1rem))] sm:rounded-2xl`): **Resumo & Assinaturas**, **Tripulantes**, **Período & Marcados**, **Fila de revisão**. Aberta do botão Fechamento de `GTManScheduleTab` e da aba Fechamento de `/department/dp`. Tipos/helpers compartilhados em `fechamento/fechamentoV2.ts` (`mesAnoAtualBRT`, `formatarDataBR`, `PendenciasPayload`, `EscalaEdicaoItem`, `EDICAO_DIFF_FIELDS`). Contrato de API em `src/app/api/gestao-tripulantes/AGENTS.md` (seção "GT Fechamento v2").
  - **ACL granular do fechamento (2026-09-18)**: 3 permissões catalogadas em `src/config/modules.ts` + seeds (`POST /api/acl/init`, grants ADMIN/MANAGER): `gestao-tripulantes.fechamento.periodo`, `.marcas`, `.revisao`. O workspace resolve `podePeriodo/podeMarcas/podeRevisao = isFechamentoRole(role) || hasFeature('<nome>')` (effective-permissions); o servidor espelha o MESMO OR nos gates (`fechamento/periodo` PUT, `fechamento/marcacoes` POST, `escala-edicoes/[id]/rejeitar|reverter` — fail-closed). A aba Fila aparece com `podeRevisao`; a Fila de revisão na página GT (`?tab=historico`) continua `mostrarSemPermissao` (todos veem, só gestores agem).
  - **Edição em tempo real na aba Tripulantes (2026-09-18)**: cada linha ganhou botão **Editar** que expande `fechamento/FechamentoEmbarquesEditor.tsx` — lista os embarques do colaborador na janela do período via `GET /api/gestao-tripulantes/embarques?colaboradorId&de&ate` e edita datas inline (`PUT /embarques/[id]`, trilha R7 gravada) / exclui com confirm (`DELETE /embarques/[id]`). Leitura para todos; edição só `podeRevisar` (server é quem decide de fato). Pós-save: refetch local + `loadPreview` (o probe de 15s cobre edições vindas de outra tela). `GET /embarques` = auth token, `de/ate` opcionais `YYYY-MM-DD`, paginado, `{success, data:{embarques}}`.
  - **R2 — Período manual por mês**: `fechamento/PeriodoFechamentoEditor.tsx` — GET/PUT `/api/gestao-tripulantes/fechamento/periodo?mesReferencia=YYYY-MM`; input dd/mm/aa com commit só em `YYYY-MM-DD` completo (mesmo gate do `ScheduleDateFilterInput`); sem registro → fonte mês civil. Badge do header mostra a fonte resolvida (`filtro` explícito > `manual` (config) > `mês civil`) e é usada por preview, aprovação e XLSX. O PUT do editor **não envia `listaConfirmada`** (flag ausente = server mantém o valor atual — R5); a badge da lista re-sincroniza pela resposta do PUT e pelo `refreshTick` do probe (confirmar/reabrir é fluxo do `MarcadosFechamentoPanel`).
  - **R5 — Marcados + lista_confirmada**: `fechamento/MarcadosFechamentoPanel.tsx` (aba Período & Marcados) — lista do mês via GET/POST `/fechamento/marcacoes` (batch upsert). `listaConfirmada=true` → **só marcados** entram naquele fechamento (badge "Confirmada — só marcados entram"); confirmar exige período definido; `false` = legado (todos que passam nos filtros). Matriz: `matrix/MarcadosBulkBar.tsx` marca em lote os selecionados (coluna de seleção desktop; gate `hasFeature('gestao-tripulantes.fechamento.marcas')` — o server reforça); mês default BRT (`mesAtualBRT`) e a barra **não** confirma a lista (isso é fluxo do fechamento).
  - **R4 — Pendências do próximo período**: `fechamento/PendenciasProximoPeriodo.tsx` — cartão informativo com `pendencias {porColaborador, totais}` de `GET /relatorio-mensal`: FI déficit da parte da folga que cai além de `data_fim`, dias de DBA além do corte (reduzem a folga do próximo período) e folga aberta sem próximo embarque (informativa, **não** gera FI — regra do dono). Nunca debita do mês atual.
  - **R7 — Fila de revisão (tudo imediato)**: `fechamento/FilaRevisaoEscala.tsx` — TODA edição de escala (POST/PUT/DELETE `/embarques`) aplica na hora (nada bloqueia) e grava trilha em `gt_escala_edicoes`; a fila lista `GET /escala-edicoes` (paginada, mais novas primeiro, filtros status/operacao/colaborador) e permite **Rejeitar**/**Reverter** com motivo obrigatório → rollback mecânico (create→soft-delete, update→restaura `dados_anteriores`, delete→remove soft-delete) e status `revertida`. O servidor recusa com 409 rollback obsoleto (edição posterior do mesmo embarque ainda `aplicada`; embarque diverge de `dados_novos` em update; un-delete com evento vivo sobreposto) — a mensagem vai para o erro da fila e o aprovador resolve a mais nova primeiro. Diff antes→depois limitado a `EDICAO_DIFF_FIELDS`. `HistoricoEmbarquesTab` mostra o histórico do colaborador (`?colaboradorId=`) com colapsáveis por evento; toasts avisam "reversível".
  - **Formulário de evento compartilhado**: `EscalaEventoForm.tsx` alimenta o painel flutuante/draggable da grade (`GTManScheduleTab`) e a edição inline da ficha (`HistoricoEmbarquesTab`) — mesmo payload do POST/PUT (`embarcacao` persiste em `local_desembarque`, `exibir_dia_inicio` controla o `d.X`).
  - **Recorte de marcações (substituição same-type)**: clique em célula JÁ MARCADA (qualquer viewport) renderiza no painel `EscalaRecorteOptions` (`EscalaEventoForm.tsx`) — checkboxes DESMARCADOS a cada abertura: "Apagar marcações anteriores/posteriores" com hint dd/mm das datas do form. Vão no POST/PUT como `apagar_anteriores`/`apagar_posteriores` (false quando ocultos): default o servidor RECORTA o evento sobreposto do mesmo tipo preservando head/tail; com a flag, o que sobra antes/depois do período salvo é apagado. Nunca mostrar em clique de célula vazia (nem em férias/afastamento, bloqueados).
  - **Exclusão com confirmação (modal, sem window.confirm)**: `ConfirmarExclusaoMarcacaoModal.tsx` (`fixed inset-0 z-[60]`, compartilhado grade + ficha). Escopo do trecho = viewport (dia → o dia clicado; semana → sáb–sex da coluna), clipado ao evento; checkbox "Apagar evento completo" só quando o evento extrapola o trecho. Confirmar → `DELETE /embarques/[id]` com `{modo:'periodo', data_inicio, data_fim}` quando parcial (sem remoção otimista — o evento pode virar 2 blocos, `fetchSchedules(force)` pinta) ou `{modo:'completo'}` (remoção otimista como antes). Bloqueio de férias/afastamento mantido; na ficha o escopo é sempre o evento inteiro (sem checkbox).
  - **Desfazer no toast**: POST/PUT/DELETE de embarques devolvem `edicoes: string[]` (ids em `gt_escala_edicoes`); se não-vazio, o toast de sucesso ganha ação "Desfazer" (react-hot-toast, auto-dismiss) que chama `POST /escala-edicoes/[id]/reverter` na ordem de desfazer do fluxo (`reverterEdicoesEmCadeia`): **POST/PUT com o evento salvo (1º id da trilha) NA FRENTE da fila** (`salvarEventoPrimeiro: true`) — em LIFO puro o un-delete do sobreposto recortado encontra o evento salvo ainda vivo e a guarda de sobreposição recusa com 409 no meio da cadeia; **DELETE em LIFO puro** (sem linha de evento salvo — fragmentos precisam morrer antes do un-delete da própria linha) — com motivo "Desfazer pelo próprio autor", para no primeiro erro mostrando a mensagem do servidor (409/403) e conclui com refetch force + toast "Desfeito". Helper em `reverter-edicoes.ts`, usado pela grade e pela ficha.
  - **Reverter/Rejeitar na ficha**: `HistoricoEmbarquesTab` mostra botões Reverter/Rejeitar nas entradas `aplicada` de operação create/update/delete/restore — visíveis só para `isFechamentoRole(profile?.role)` (`SupabaseAuthContext`, mesmo critério da `FilaRevisaoEscala`; o servidor reforça). Clique abre mini-form inline de motivo obrigatório → `POST /escala-edicoes/[id]/rejeitar|/reverter` → recarrega embarques (`onRefresh`) + trilha; erro (409 rollback obsoleto etc.) vai para o toast.
- **Histórico global de alterações (aba da página GT, fora do workspace)**: `tabs/HistoricoEdicoesTab.tsx` monta a aba **Histórico de alterações** de `/department/gestao-tripulantes` (`?tab=historico`, ícone `FiClock`, visível para quem já vê a página) sobre a MESMA `FilaRevisaoEscala` do workspace — sem duplicar diff/rollback. Props novas da fila: `mostrarSemPermissao` (lista visível para todos, somente leitura + banner; no workspace continua escondida sem permissão), `statusInicial=''` (histórico mostra todos os status; a fila do workspace segue default `aplicada`), `titulo`/`descricao` sobrescritos e empty-state próprio (`gtFechV2.historicoGlobal.vazio` — o `fila.vazio` fala de "fila"). Filtros novos na fila compartilhada: **operação** (GET `operacao=create|update|delete|restore|rejeicao|reversao`) e **período de/até** (`ScheduleDateFilterInput`, commit só em `YYYY-MM-DD` completo; GET `de=`/`ate=`). O recorte de período é **server-side** (`GET /escala-edicoes` aplica janela BRT inclusiva em `created_at`; período inválido/invertido → 400 com mensagem) — o cliente só envia os params e NÃO re-filtra a página (counts/paginação exatos). Reverter/Rejeitar (motivo obrigatório) só para `isFechamentoRole(profile?.role)`; erro 409/403 → toast com a mensagem do server; sucesso → toast + refetch da lista.
- **R3 — Live probe 15s**: dois hooks sobre `GET /api/gestao-tripulantes/live-probe?escopo=escala|fechamento|colaboradores` (poll 15s, fail-soft, pausa com `document.hidden`, baseline na 1ª resposta): `src/hooks/useGtLiveProbe.ts` (expõe `assinatura`/`verificarAgora`; workspace do fechamento refetch ao mudar) e `use-gt-live-probe.ts` (local, com `canProbe` para pausar durante save/refetch e `notifyLocalWrite` para re-basar após gravação própria — o próprio save nunca dispara refetch redundante; usado por `GTManScheduleTab` e `/department/man-schedule`). Probe complementa — nunca substitui — os guards de geração/stale-guard existentes.
- **R8 — Multi-embarcação nos filtros**: Matriz `GTMatrixFilters` (chips multi, valores = **NOMES** — API resolve nomes→ids; query `embarcacoes=csv`), Man Schedule `MultiVesselSelect.tsx` (opções = GET `/embarcacoes` + embarcações presentes nos dados; Set de nomes exatos case-insensitive via `vesselMatchesSelection`; header da grade via `formatVesselHeaderName`, lista até 3 + `+N`), workspace do fechamento `fechamento/MultiEmbarcacaoFilter.tsx` (chips; `embarcacoes` tem prioridade sobre `embarcacao` único e vai no XLSX).
- **R9 — Utilitários mobile (portal inteiro)**: `globals.css` ganhou `.pb-safe`/`.pt-safe` (safe-area insets — requer `viewportFit: 'cover'` no `export const viewport` de `src/app/layout.tsx`), `.tap-target` (alvo de toque mínimo 44×44 — botões de menu mobile de `MainLayout` e `src/app/admin/layout.tsx`) e `.table-responsive` (swipe horizontal com momentum iOS + scrollbar fina). Admin: footer visível no mobile com `pb-safe` e backdrop do drawer `fixed inset-0 bg-black/50 z-20 md:hidden`; `MainLayout` tem o mesmo backdrop (`z-30`) e header sticky com `backdrop-blur-md`.

## Work Guidance

- Não voltar `<select>` nativo nesses quatro campos de cadastro.
- Não gravar escala no MIO.
- Não ligar `onChange` de `type="date"` direto em `setFilterDate*` da grade.
- Não usar `border-collapse` na tabela da escala (quebra sticky de nomes/semanas).
- Não juntar `overflow-x-auto` + `min-w-max` no mesmo nó do tablist do CollaboratorModal (scrollbar nativo gigante no Windows). Shell = scroll; inner = `min-w-max`. Overlay do modal sem `overflow-y-auto`.
- Não usar alturas mágicas (`calc(100vh-6.5rem)`) nas páginas GT-family — `GtPageShell` + flex `min-h-0` / `overflow-auto`.
- Não mudar overflow/sticky em `man-schedule-grid-classes.ts` só para o chrome da página.
- Não remover `min-w-0` da cadeia flex da grade (`<main>`, `GtPageShell`, wrapper da aba, root do tab, `[data-testid=man-schedule-scroll]`). Sem isso a tabela `w-max` estica o pane e o scroll horizontal some.
- Não trocar o scrollport da grade por `overflow-x-only` — linhas também transbordam; eixos X e Y juntos (`overflow-x-scroll overflow-y-auto`).
- Não gate de aprovação no save de evento (R7 = tudo imediato): a fila de revisão é pós-fato com rollback; bloquear o save ou fingir "pendente de aprovação" quebra o contrato.
- Não apagar/editar linhas de `gt_escala_edicoes` pela UI (trilha append-only); reversão só pelos endpoints `rejeitar`/`reverter`.
- Não usar `fetchWithToken` no probe — fetch cru + `getToken` (sem `console.error` durante o rollout do endpoint); probe não roda com aba oculta nem durante save em voo (`canProbe`).
- Não confirmar a lista de marcados fora do fluxo do fechamento (`MarcadosBulkBar` marca pessoas; `listaConfirmada` só no workspace, e exige período definido).

## Verification

- Editar Dados Pessoais: form completo (abas pessoais/docs/banco/vínculo/e-Social). Digitar cargo inexistente → Adicionar → POST 201 → campo fica com o novo id. Selecionar **Sem escala** → Salvar → reload ainda mostra Sem escala (não 14x14).
- DP **Novo colaborador** abre `/department/dp/novo` e grava `POST /colaboradores` (CPF válido).
- Cards da Matriz < total de linhas se existirem inativos.
- KPI 1 vencido: card/filtro lista o documento; linha abre ficha; aba correta mostra o card (incl. certificado/CNH/laudo). ASO abre a aba ASO, não QHSE.
- Modal GT com módulo `epi`: aba **QHSE / EPI** lista ficha AN-HSE-005 e listas QHSE e **não** lista ASO/laudo.
- Clique em "Embarcados Agora" filtra a lista ao mesmo conjunto do card (ON hoje, sem *). Quem está na lista com ON hoje mostra pílula **Embarcado** (não Folga stale).
- Admin Fechamento: dropdown lista usuários ativos (USER incluso), não só ADMIN/MANAGER. USER na lista consegue assinar; ADMIN fora da lista toma 403. `npx tsx --test src/lib/gestao-tripulantes/fechamento-assinatura.test.ts`.
- GT aba ASO Logística lista solicitações do DP; aprovar com assinatura → status Marcado; reprovar mostra motivo no DP. USER do setor Logística com módulo GT passa no POST aprovar; USER de outro setor com o mesmo módulo toma 403.
- Man Schedule: checkbox liga colunas diárias; desliga volta semana. Badge `Hoje: NP a bordo` interpola `{count}` e usa a mesma regra POB (ON exato no dia civil de hoje), mesmo com mês de referência futuro. **Hoje** volta ao mês atual e à coluna de hoje; setas de coluna andam uma coluna; setas do mês mudam o mês de referência e geram colunas mesmo sem rotações. `npx tsx --test src/i18n/interpolate.test.ts src/lib/gestao-tripulantes/embarque-status.test.ts src/lib/gestao-tripulantes/man-schedule-nav.test.ts`.
- Man Schedule (aba GT **e** `/department/man-schedule`): `scrollWidth > clientWidth` no `[data-testid=man-schedule-scroll]`; barra inferior ~14px; barra superior move a grade; swipe horizontal no mobile sem esticar a sidebar. Nomes (`man-schedule-sticky-name`) permanecem visíveis; cabeçalho de semanas fica no topo. `node --test src/components/gestao-tripulantes/man-schedule-grid-classes.test.mjs`.
- Matriz GT / aba ASO Logística / `/department/man-schedule`: o documento não rola; filtros ficam visíveis; a lista/grade rola no pane restante (`[data-testid=gt-page-shell]`).
- CollaboratorModal: painel ~96dvh; header+abas visíveis; tablist sem barra nativa grossa (`[data-testid=collaborator-modal-tablist]`); cada aba rola a lista/formulário no espaço restante.
- Ficha unificada: card **Usuário do portal** (`[data-testid=ficha-portal-user]`) mostra nome + e-mail + role quando o hub resolve `users_unified` por `user_id` / `tax_id` / e-mail; sem match → "sem vínculo".
- Após Salvar ON, as células do período e a coluna ON atualizam sem recarregar a página.
- Digitar o ano em Data Início (`2`, `20`, `202`) não congela a UI; a grade só muda com data completa ou picker.
- Treinamentos: CBSP válido + CBSP vencido/declaração → uma linha primária, Histórico colapsado, resumo sem “1 vencido” falso.
- Card `total_docs_vencidos` da Matriz não sobe por declaração/certificado antigo se o primário do grupo está válido.
- USER sem feature JSONB **e** sem ACL `gestao-tripulantes.documents.edit`/`.delete` (nem manage/admin) não vê os botões de editar/excluir em Treinamentos/ASO/Documentos/Passaportes. ADMIN/MANAGER sempre veem. Grant via checkbox de feature **ou** árvore ACL em `/admin/users` faz o botão aparecer (`useGtDocumentPermissions` + `GET .../documentos/permissions`) e o `PUT`/`DELETE` passar. Após o admin salvar, o alvo precisa de refresh/re-login ou voltar à aba: o próximo fetch lê DB+ACL (não JWT).
- ASO com e-Social `enviado`/`processado`: cartão mostra "Já enviado ao e-Social — não editável" em vez dos botões Editar/Excluir.
- CollaboratorModal: USER ADMIN/MANAGER (ou DP com módulo GT) vê **Desligar**; após confirmar, `ativo=false` no estado local e a aba Desligamento mostra o histórico. Já desligado não abre o wizard de novo.
- **Matriz de Treinamentos (Visibilidade por ACL e Setor)**:
  - Componente `MatrizTreinamentoConfigTab.tsx` suporta prop `readOnly` para usuários que possuem permissão de visualização sem edição.
  - No departamento (`/department/gestao-tripulantes`), a aba **Matriz de Treinamentos** e o botão no header usam o hook `useGtMatrizPermissions()`.
  - Só são visíveis quando o usuário possui acesso concedido via Setor (DP, RH, Treinamento, SMS/QHSE, Operações com módulo GT), Role (ADMIN/MANAGER), ACL (`gestao-tripulantes:matrizes.manage` ou `.view`) ou Feature JSONB (`gestao-tripulantes.matrizes.manage`). Usuários sem permissão não veem a aba nem o botão.
- **GT v2 (Fechamento/Escala/Matriz/Mobile)**:
  - Workspace do fechamento abre tela cheia no mobile (`h-[100dvh]`, sem borda) com 4 abas; **Fila de revisão** só aparece para ADMIN/MANAGER (`isFechamentoRole`). Rejeitar um `create` deixa o evento de sair da grade (rollback) e a linha vira `revertida` com `revisada_por_*`; editar/excluir um evento mostra toast "reversível" e o histórico em `HistoricoEmbarquesTab` lista a trilha com diff antes→depois.
  - Aba **Histórico de alterações** na página GT (`?tab=historico`): lista a trilha inteira (status default = todos) e rola no pane restante do `GtPageShell` (sem alturas mágicas). USER vê a lista + banner "somente gestores" e NÃO vê Reverter/Rejeitar; ADMIN/MANAGER vê os botões, motivo obrigatório, 409/403 no toast e refetch após sucesso. Filtros de operação e período de/até regarregam da página 1.
  - Recorte/exclusão/desfazer: clicar numa célula marcada mostra os checkboxes de recorte DESMARCADOS (hint dd/mm); Excluir abre modal (sem `window.confirm`) com tripulante + trecho do viewport e checkbox "Apagar evento completo" só quando parcial — parcial não faz remoção otimista (o bloco pode virar 2) e refetcha com force. Após save/excluir com `edicoes[]` na resposta, o toast oferece Desfazer (reverter em ordem reversa, para no 1º erro com a mensagem do server). Na ficha, gestores (ADMIN/MANAGER) veem Reverter/Rejeitar com motivo obrigatório nas entradas `aplicada`; USER não vê nada.
  - Período & Marcados: definir período (dd/mm/aa) + Confirmar lista → badge vira "Confirmada — só marcados entram" e preview/XLSX passam a listar só os marcados; Confirmar fica bloqueado sem período definido. Pendências do próximo período aparecem no cartão (FI/DBA/folga aberta) sem debitar do mês atual.
  - Live probe: com a grade aberta em duas abas, salvar evento numa faz a outra refetch em ≤15s; o save local não dispara refetch redundante (`notifyLocalWrite`). `npx tsx --test src/i18n/interpolate.test.ts`.
  - Multi-embarcação: Matriz e Man Schedule filtram 2+ embarcações ao mesmo tempo (`embarcacoes=csv`); chips no cabeçalho da grade (`formatVesselHeaderName` até 3 + `+N`) e no workspace do fechamento.
  - Mobile shell: `.pb-safe`/`.pt-safe` respeitam a home indicator (viewport `viewportFit: 'cover'`), botões de menu com `.tap-target` (44×44), tabelas empilham swipe horizontal via `.table-responsive`; footer do admin visível no mobile.

## Child DOX Index

- `src/lib/document-catalog/AGENTS.md` — catálogo global; aba QHSE / EPI do modal (módulo `epi`); QHSE nunca lista ASO
