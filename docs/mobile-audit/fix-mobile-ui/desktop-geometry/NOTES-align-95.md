# Geometria #99 alinhada à #95

Base: `origin/portal` `71bd3534` (`:3020`). HEAD: working tree desta rodada (`:3051`). Compile path `/tmp/geom-compile`. Viewports 1024×768, 1280×800, 1440×900. Confirmation + LanguageDialog abertos.

## Boxes

- Confirmation e Language: iguais nos 3 viewports. Sem X no Confirmation desktop (`mountOnlyWhenMobile`).
- Shell, escala, tablist da ficha, login: iguais.
- `ficha` `modalClose`: HEAD 32×32, portal `—`. Já em `e08b9714`. O X do `CollaboratorModal` existia no portal; `#99` só pôs `data-modal-close` no botão. O coletor não vê o X do portal. Painel/tablist iguais.

## px residual

688–1667 = Companion/ícone + AA. Login 0–38 (`maxChannel` 37). Confirmation 0–7 (`maxChannel` 1).

## Mobile

Confirmation X 44×44 em 390 e 375; ausente em 1280. `?tab=schedule` monta a escala sem clique. Esc fecha Confirmation/Desligamento/Fechamento/AddShortcut.
