# Geometria #108 News

Base `85f11029` (`:3060`) vs HEAD desta rodada (`:3071`). Path `/tmp/geom-compile`. Viewports 1024/1280/1440. NewsPostEditorFullScreen aberto.

Todos os boxes iguais. px: 0 exceto dashboard 1440 = 61 (`maxChannel` 2). Helpers = #99 `807a7e48`.
