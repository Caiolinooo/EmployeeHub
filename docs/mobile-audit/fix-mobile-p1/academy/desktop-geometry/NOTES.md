# Geometria #111 Academy

Base `85f11029` (`:3060`) vs HEAD (`:3074`). Path `/tmp/geom-compile`.

DeleteCourseModal, ModuleEditor e QuizEditor. Boxes iguais nos 3 viewports. px 0–61 AA.
Mobile 390/375: Delete X 44×44, Esc 2→0.
